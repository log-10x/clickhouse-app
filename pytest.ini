[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q --strict-markers
markers =
    integration: end-to-end tests that load real sample data into ClickHouse
    unit: golden-case decode tests
